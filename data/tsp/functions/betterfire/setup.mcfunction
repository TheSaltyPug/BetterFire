#> This function will run on datapack loading
# no scores for this one, so this is pretty much empty and no need for uninstall function

# since we don't need to run every tick, we can optimize by scheduling
schedule function tsp:betterfire/check_arrows 5t
